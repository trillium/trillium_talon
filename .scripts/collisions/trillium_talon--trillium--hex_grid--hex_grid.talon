# tag: browser
# and tag: user.rango_direct_clicking
# -
        # hex (grid | toggle):
        #     # user.hud_add_log("warning","hex grid")
        #             user.hex_toggle()

        # hex on:
        #     # user.hud_add_log("warning","hex on")
        #     user.hex_toggle("on")

        # hex off:
        #     # user.hud_add_log("warning","hex off")
        #     user.hex_toggle("off")

        # hex <user.hex_target>:
        #     user.test_test(hex_target)
        #     # sleep(200ms)
        #     # user.hud_add_log('warning', 'hex_target')
        # # ^<user.rango_target>$: user.rango_command_with_target("directClickElement", rango_target)
        # # ^<user.rango_hint_double> (twice | second)$: user.rango_command_with_target("directClickElement", rango_hint_double)

        # hex {user.hex_grid_coord_name}:
        #     user.hud_add_log("success", hex_grid_coord_name)